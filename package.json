{
  "name": "react-hooks-import-export-lab",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "echo \"results.json\" && react-scripts test --reporters=@learn-co-curriculum/jest-learn-reporter",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@learn-co-curriculum/jest-learn-reporter": "^0.1.0",
    "react-scripts": "^5.0.1"
  }
}
